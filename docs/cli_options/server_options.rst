==============
Server Options
==============

.. program:: rotest server

You can run the server using command :command:`rotest server`.
The command by default runs Django's server with the port supplied
in the rotest.yml file, defaults to 8000.
